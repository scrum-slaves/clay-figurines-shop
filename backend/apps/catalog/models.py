from django.db import models


# TODO: добавить модели ProductType, Product, ProductImage.
# Важно: медиа предполагается хранить в БД (например, BinaryField/bytea),
# поэтому image-поле и стратегию хранения добавьте на этапе проектирования модели.
